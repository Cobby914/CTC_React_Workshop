import { Box, Heading } from "@chakra-ui/react";

// InterestsSection
// TODO: Show your interests or hobbies.
// Requirements:
// - Create an array of interests
// - Use .map() to render cards or items for each interest
// - Use a layout component such as SimpleGrid, Stack, or Flex
const InterestsSection = () => {
  return (
    <Box mt={10}>
      <Heading size="md" mb={3}>
        Interests & Hobbies
      </Heading>
      {/* Replace this with your interests layout */}
    </Box>
  );
};

export default InterestsSection;
