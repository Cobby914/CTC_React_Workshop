import { Box, Heading } from "@chakra-ui/react";

// SkillsSection
// TODO: Show a list of your skills.
// Requirements:
// - Create an array of skills (e.g., const skills = ['React', 'Chakra', 'JS'])
// - Use .map() to render them
// - Consider creating a separate SkillTag component for individual skills
const SkillsSection = () => {
  return (
    <Box mt={10}>
      <Heading size="md" mb={3}>
        Skills
      </Heading>
      {/* Replace this with your skills list */}
    </Box>
  );
};

export default SkillsSection;
